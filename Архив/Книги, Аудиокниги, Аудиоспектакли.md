


```dataviewjs

	const iconBook1 = "&#128366";
	const iconBook2 = "&#x1F56E";
	
	const iconHeadphone1 = "&#127911";
	const iconHeadphone2 = "&#x1F3A7";
	
	let booksData;
	let bookClass;
	let bookFooter;
	let booksCSS = "";
	
	await dv.header(1, `<div id='filters'><div data-f='reading'>${iconBook1} Читаю</div><div data-f='listening'>Слушаю ${iconHeadphone1}</div></div>`);

	booksData = dv.pages('"Знания_Информация_Контент/Литература"').where(page => {
		if (page.Читаю || page.Слушаю) return page;
	});
	
	console.log('booksData', booksData);
	
	for (let book of booksData) {
		// book.Читает ? bookFooter = `Читает: ${book.Читает}` : bookFooter = book.ISBN;
		
		if (book.Читает) bookClass = "listening";
		else bookClass = "reading";
		
		booksCSS += `<div class="book ${bookClass}">
			<div class="bookHeader">
				<div class="title">${book.file.name}</div>
				<div class="author">${book.Автор}</div>
			</div>
			<div class="bookCover">
				<a href="${book.file.path}" class="internal-link" aria-label="${book.Название}"><img src="${book.Обложка}" alt="" /></a>
			</div>
			<div class="bookFooter">
				<progress max='${book.ПрогрессMax}' value='${book.Прогресс}'></progress>
				<div>
					<div>${book.Прогресс}</div>
					<div>${book.Год} год</div>
					<div>${book.ПрогрессMax}</div>
				</div>
			</div>
		</div>`
	}
	
	let test = document.querySelector("#filters").addEventListener("click", event => {
		let filterClass = event.target.dataset['f'];
		
		document.querySelectorAll('.' + filterClass).forEach(item => {		
			if (item.classList.contains('hide')) {
				event.target.style.color = "green";
				item.classList.remove('hide');
			} else {
				event.target.style.color = "gray";
				item.classList.add('hide');
			}
		});			
	});
	
	dv.el("div", booksCSS, {cls: "books"});
		
```

---

# К прочтению | Для прослушивания

```dataviewjs
	
	const iconBook1 = "&#128366";
	const iconBook2 = "&#x1F56E";
	
	const iconHeadphone1 = "&#127911";
	const iconHeadphone2 = "&#x1F3A7";
		
	let bookFooter;
	let booksCSS = "";

	///let booksData = dv.pages('"Просвещение/Книги"').where(page => {
	//	if (page.читаю == false && page.прочитано == false) return page;
	//});
	
	await dv.header(1, `<div id='filters'><div data-f='reading'>${iconBook1} К прочтению</div><div data-f='listening'>Для прослушивания ${iconHeadphone1}</div></div>`);
	
	let booksData = dv.pages('"Просвещение/Книги" or "Просвещение/Аудиокниги"').where(page => {
		if ((page.читаю == false && page.прочитано == false) || (page.Слушаю == false && page.Прослушано == false)) return page;
	}).sort(page => page.file.ctime, "desc");

	for (let book of booksData) {	
		booksCSS += `<div class="book">
			<div class="bookHeader">
				<div class="title">${book.file.name}</div>
				<div class="author">${book.Автор}</div>
			</div>
			<div class="bookCover">
				<a href="${book.file.path}" class="internal-link" aria-label="${book.Название}"><img src="${book.Обложка}" /></a>
			</div>
			<div class="bookFooter">
					<p style="text-align: center; margin: 0; padding: 0;">${book.Год} год</p>
			</div>
		</div>`
	}
	
	dv.el("div", booksCSS, {cls: "books"})
```

---

# Прочитано | Прослушано

```dataviewjs
	
	let bookFooter;
	let booksCSS = "";

	let booksData = dv.pages('"Просвещение/Книги" or "Просвещение/Аудиокниги"').where(page => {
		if (page.прочитано || page.Прослушано) return page;
	}).sort(page => page.file.ctime, "desc");


	for (let book of booksData) {
		book.Читает ? bookFooter = `Читает: ${book.Читает}` : bookFooter = book.ISBN;
		
		booksCSS += `<div class="book">
			<div class="bookHeader">
				<div class="title">${book.file.name}</div>
				<div class="author">${book.Автор}</div>
			</div>
			<div class="bookCover">
				<a href="${book.file.path}" class="internal-link" aria-label="${book.Название}"><img src="${book.Обложка}" /></a>
			</div>
			<div class="bookFooter">
				<div>${bookFooter}</div>
			</div>
		</div>`
	}
	
	dv.el("div", booksCSS, {cls: "books"})
```
