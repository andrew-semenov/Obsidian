---
Это: Полезное
🔵: Программы
---
Чтобы начать вводить текст в Vim, нажмите клавишу 

**`i`** (режим _Insert_).

Если же вы имели в виду, как вообще **открыть файл** для редактирования из терминала, используйте команду:  
`vim название_файла`

**Шпаргалка для старта:**

- **`i`** — войти в режим вставки (набирать текст).
- **`Esc`** — вернуться в командный режим (чтобы отдавать приказы редактору).
- **`:w`** — сохранить изменения.
- **`:q!`** — выйти без сохранения (экстренный выход).
- **`:wq`** — сохранить и выйти.

Хотите узнать, как быстро **перемещаться** по тексту без стрелочек, или помочь с **настройкой** конфига?

Чтобы не тянуться к стрелочкам (это считается «грехом» в мире Vim), используйте клавиши в 

**командном режиме** (нажмите `Esc`, если вы в режиме вставки):

- **`h` `j` `k` `l`** — влево, вниз, вверх, вправо соответственно. Пальцы всегда остаются на основном ряду клавиатуры.
- **`w`** (word) — прыжок к началу следующего **слова**.
- **`b`** (back) — прыжок к началу предыдущего **слова**.
- **`0`** (ноль) — прыгнуть в самое **начало строки**.
- **`$`** — прыгнуть в самый **конец строки**.
- **`gg`** — в начало файла.
- **`G`** — в конец файла.

**Мастер-фишка:** Если нажать цифру перед командой, она повторится. Например, **`10j`** пролистает текст на 10 строк вниз.

Показать, как мгновенно **найти** нужное слово в тексте или перейти на **конкретную строку**?

Конечно! Это как раз те команды, которые превращают Vim из «странного блокнота» в мощный инструмент.

Все команды ниже выполняются в **нормальном режиме** (после нажатия `Esc`).

1. Поиск слова

- **`/слово`** — наберите слэш, затем текст, который ищете, и нажмите `Enter`. Vim подсветит совпадения.
- **`n`** (next) — перейти к **следующему** найденному совпадению.
- **`N`** — вернуться к **предыдущему** совпадению.
- **`*`** (звездочка) — если курсор уже стоит на слове, нажмите `*`, и Vim мгновенно найдет следующее такое же слово в файле.

2. Прыжки по строкам

- **`:число`** — например, набрав `:42` и нажав `Enter`, вы мгновенно окажетесь на 42-й строке.
- **`ctrl + f`** — прокрутка на страницу **вниз** (Forward).
- **`ctrl + b`** — прокрутка на страницу **вверх** (Backward).

---

Бонус: Как это объединить?

Представьте, что вам нужно удалить 3 слова впереди. Вместо того чтобы жать _Backspace_ 20 раз, вы просто нажимаете:

> **`3dw`** (`3` — количество, `d` — delete, `w` — word)

Маленький совет по настройке

Чтобы всегда видеть номера строк (это очень помогает прыгать по ним), введите в Vim команду:  
`:set number`

Хотите, я покажу, как сохранить эту настройку навсегда, чтобы не вводить её каждый раз при запуске?

Задать вопрос по теме:




5 сайтов
Чтобы настройки (например, номера строк) применялись автоматически при каждом запуске, нужно создать специальный конфигурационный файл.
1. Как создать файл настроек
В терминале введите команду:
bash
vim ~/.vimrc
Используйте код с осторожностью.

(Если вы на Windows, файл обычно называется _vimrc и лежит в папке пользователя). 
Reddit
Reddit
 +1
2. Что туда написать
В открывшемся файле нажмите i (режим вставки) и добавьте те опции, которые вам нравятся. Вот «джентльменский набор» для начала: 
Quora
Quora
 +1
vim
set number          " Включить нумерацию строк
set relativenumber  " Относительная нумерация (удобно для прыжков типа 10j)
set mouse=a         " Разрешить использование мыши (иногда полезно)
set syntax=on       " Включить подсветку синтаксиса
set tabstop=4       " Размер табуляции — 4 пробела
set shiftwidth=4    " Размер отступа при автодополнении
set expandtab       " Превращать табы в пробелы
set hlsearch        " Подсвечивать результаты поиска
set incsearch       " Искать текст сразу при наборе
Используйте код с осторожностью.

3. Как сохранить
После того как вписали настройки:
Нажмите Esc.
Введите :wq и нажмите Enter. 
Beget
Beget
Готово! Теперь при каждом запуске Vim будет выглядеть и вести себя так, как вы настроили.
Хотите узнать, как изменить цветовую схему на что-то более симпатичное или как установить плагины, чтобы сделать из Vim настоящую IDE

9 сайтов
Чтобы сделать Vim красивым и мощным, мы поработаем с темами и установим менеджер плагинов.
1. Темы (Colorschemes)
Vim уже имеет встроенные темы. Чтобы попробовать их, введите команду:
:colorscheme [пробел] [Tab] — перебирайте варианты клавишей Tab. Популярные встроенные: desert, ron, slate. 
Stack Overflow
Stack Overflow
 +1
Для постоянной смены темы добавьте строку в свой ~/.vimrc:
vim
colorscheme desert
Используйте код с осторожностью.

Самые популярные внешние темы (нужно скачивать отдельно):
Gruvbox (ретро-стиль, очень мягкий для глаз).
Dracula (яркая темная тема).
Solarized (классика с идеальным контрастом


Установка плагинов (IDE-фишки)
Чтобы не устанавливать каждый плагин вручную, используют менеджеры, например vim-plug. 
Reddit
Reddit
 +1
Как настроить:
Скачайте plug.vim (одной командой из интернета).
Добавьте в ~/.vimrc блок для плагинов:
vim
call plug#begin()
Plug 'preservim/nerdtree'       " Дерево файлов слева
Plug 'vim-airline/vim-airline'  " Красивая статусная строка внизу
Plug 'morhetz/gruvbox'          " Тема Gruvbox
call plug#end()
Используйте код с осторожностью.

Перезапустите Vim и введите команду :PlugInstall. 
YouTube
YouTube
 +1
Полезные плагины для старта:
NERDTree: открывает боковую панель с файлами (команда :NERDTreeToggle).
Vim-airline: показывает режим, ветку Git и позицию курсора в стильной полоске.
fzf.vim: мгновенный поиск файлов по названию. 
Stack Overflow
Stack Overflow
 +1
Совет: Если вы чувствуете, что настройка Vim становится слишком сложной, многие переходят на Neovim, где всё это работает быстрее и современнее.
Хотите попробовать настроить боковую панель с файлами или сразу перейдем к магии Neovim



6 сайтов
Neovim (nvim) — это современный «заряженный» форк Vim, который сохранил все его команды, но стал быстрее и гибче. Если Vim — это надежный внедорожник, то Neovim — это тот же внедорожник, но с бортовым компьютером и мощным тюнингом. 
Reddit
Reddit
 +2
Почему все переходят на Neovim?
Lua вместо VimScript: В Neovim конфиги пишутся на языке Lua. Он гораздо быстрее, понятнее и современнее старого VimScript.
Встроенный LSP (Language Server Protocol): Это киллер-фича. Neovim «из коробки» понимает ваш код как полноценная IDE: подсказки, автодополнение и переход к определениям работают молниеносно.
Treesitter: Это движок, который делает подсветку синтаксиса невероятно точной и быстрой.
Асинхронность: Плагины в Neovim не «фризят» редактор во время работы (например, при проверке ошибок в коде). 
Neovim
Neovim
 +2
Как быстро попробовать (Готовые сборки)
Настраивать Neovim с нуля на Lua может быть долго. Поэтому новички часто используют готовые конфигурации («дистрибутивы»), которые делают его похожим на VS Code за 1 минуту:
NvChad: Очень быстрый, красивый и минималистичный.
AstroNvim: Мощный, с кучей встроенных инструментов.
LazyVim: Сейчас самый популярный — очень гибкий и современный. 
YouTube
YouTube
Твой первый шаг в Neovim:
Установи его: brew install neovim (macOS) или через ваш менеджер пакетов в Linux.
Создай конфиг: Вместо ~/.vimrc теперь используется файл ~/.config/nvim/init.lua.
Хочешь, помогу написать твой первый конфиг на Lua или сразу попробуем установить одну из готовых сборок, чтобы всё летало?



Neovim - Wikipedia

Wikipedia

What is neovim used for? : r/linux4noobs - Reddit

Reddit



Чтобы не тратить часы на ручную настройку каждой запятой, давай поставим LazyVim. Это самый «умный» способ: ты получишь интерфейс как в топовых IDE, но со скоростью Neovim.
Как установить всё за 2 минуты:
Установи Neovim (если ещё нет):
macOS: brew install neovim
Linux: sudo apt install neovim (или через ваш менеджер пакетов)
Сделай бэкап старых настроек (на всякий случай):
bash
mv ~/.config/nvim ~/.config/nvim.bak
Используйте код с осторожностью.

Клонируй шаблон LazyVim:
bash
git clone https://github.com ~/.config/nvim
Используйте код с осторожностью.

Запусти: просто введи в терминале nvim.
Что ты получишь сразу:
Проводник файлов: нажми <Space> e (пробел, потом e), чтобы открыть дерево файлов слева.
Поиск файлов: нажми <Space> ff для мгновенного поиска по названию.
Умное меню: нажми и подожди секунду клавишу Space — внизу всплывет подсказка со всеми возможными командами. Тебе больше не нужно заучивать всё наизусть! [1, 2]
Маленький нюанс (Важно!):
Для красивых иконок тебе понадобится Nerd Font (специальный шрифт). Без него вместо значков папок будут странные квадраты. Популярный выбор — JetBrainsMono Nerd Font.
Попробуем поставить крутой шрифт или разберемся, как в этой сборке включить автодополнение для твоего языка программирования


