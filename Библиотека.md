


```dataviewjs

	const iconBook1 = "&#128366";
	const iconBook2 = "&#x1F56E";
	
	const iconHeadphone1 = "&#127911";
	const iconHeadphone2 = "&#x1F3A7";
	
	let booksData;
	let bookClass;
	let bookFooter;
	let booksCSS = "";
	
	
	let renderedView = [];
	//await dv.header(1, `<div id='filters'><div data-f='reading'>${iconBook1} Читаю</div><div data-f='listening'>Слушаю ${iconHeadphone1}</div></div>`);
	
	await dv.header(1, `<input type="checkbox" name="reading" value="readval">
						<label for="reading">в процессе</label>
						<input type="checkbox" name="listening" value="listval">
						<label for="listening">ждет</label>
						<input type="checkbox" name="archive" value="archval">
						<label for="archive">Архив</label>
					`);


document.querySelector('input[type="checkbox"][name="reading"]').addEventListener("change", async (event)=>{
	console.log("В процессе:", event.currentTarget.checked);

	if (event.currentTarget.checked) {
		
	//booksData;
	booksCSS = "";
	
		
		booksData = await dv.pages('"📚🎧 Литература/Книги и Аудиоспектакли"').where(page => {
		   if (page.Статус === "В процессе") return page;
	    });
	
		console.log('booksData', booksData);
		
	   	for (let book of booksData) {		
			booksCSS += `<div class="spine">
						<span class="author">Книга</span><br/>
						<span class="title">${book.file.name}</span>
						</div>`;
		}
	
		renderedView = dv.el("div", booksCSS, {cls: "books-proc"});
		console.log('renderedView: ', renderedView);
	} else {
		
		let els = document.getElementsByClassName('books-proc');
		//console.log("els", els);

		for (let el of els) {
			console.log("el", el);
			el.innerHTML = '';
			el.remove();
		}

		// booksData = []
		// booksCSS = ``;
		//dv.el("div", "");
		//console.log('renderedView: ', renderedView);
	}
	
	//console.log('booksCSS', booksCSS);
	//dv.el("div", booksCSS, {cls: "books"});
})

document.querySelector('input[type="checkbox"][name="listening"]').addEventListener("change", event=>{
	console.log("Ждёт:", event.currentTarget.checked);
	
		if (event.currentTarget.checked) {
		
		booksCSS = "";
		
		booksData = dv.pages('"📚🎧 Литература/Книги и Аудиоспектакли"').where(page => {
		   if (page.Статус === "Ждёт") return page;
	    });
	
		console.log('booksData', booksData);
		
	   	for (let book of booksData) {		
		booksCSS += `<div class="spine">
					<span class="author">Фёдор Достоевский</span><br/>
					<span class="title">${book.file.name}</span>
					</div>`
		}
	
		dv.el("div", booksCSS, {cls: "books"});
	} else {
		const els = document.getElementsByClassName('books');
		//console.log("els", els);
		for (let el of els) {
			console.log("el", el);
			el.remove();
		}
	}
})

document.querySelector('input[type="checkbox"][name="archive"]').addEventListener("change", event=>{
	console.log("Архив:", event.currentTarget.checked);
})
 
 



	/*booksData = dv.pages('"📚🎧 Литература/Книги и Аудиоспектакли"').where(page => {
	       return page;
	   });
	
	console.log('booksData', booksData);
	
	for (let book of booksData) {
		// book.Читает ? bookFooter = `Читает: ${book.Читает}` : bookFooter = book.ISBN;
		
		//if (book.Читает) bookClass = "listening";
		//else bookClass = "reading";
		
		booksCSS += `<div class="spine">
					<span class="author">Фёдор Достоевский</span>
					<span class="title">${book.file.name}</span>
					</div>`
	}*/
	
	
	/*let test = document.querySelector("#filters").addEventListener("click", event => {
		let filterClass = event.target.dataset['f'];
		
		document.querySelectorAll('.' + filterClass).forEach(item => {		
			if (item.classList.contains('hide')) {
				event.target.style.color = "green";
				item.classList.remove('hide');
			} else {
				event.target.style.color = "gray";
				item.classList.add('hide');
			}
		});			
	});*/
	
	//dv.el("div", booksCSS, {cls: "books"});
		


```


---

<div class="spine">
  <span class="author">Фёдор Достоевский</span>
  <span class="title">Преступление и наказание</span>
</div>



.spine {
  /* Горизонтальный корешок */
  width: 400px;
  height: 45px;
  background: #4a2c2a; /* Глубокий коричневый */
  
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 0 25px;
  box-sizing: border-box;
  
  /* Эффект объема (свет падает сверху) */
  background: linear-gradient(180deg, 
    rgba(0,0,0,0.4) 0%, 
    rgba(255,255,255,0.15) 45%, 
    rgba(255,255,255,0.15) 55%, 
    rgba(0,0,0,0.4) 100%),
    #4a2c2a;
    
  box-shadow: 0 10px 20px rgba(0,0,0,0.4);
  border-radius: 0 5px 5px 0;
  position: relative;
}

/* Стили для Автора */
.author {
  color: #d4af37; /* Золотистый цвет */
  font-family: "Georgia", serif;
  font-size: 14px;
  font-style: italic;
  letter-spacing: 1px;
}

/* Стили для Названия */
.title {
  color: #f1e5d1;
  font-family: "Verdana", sans-serif;
  font-weight: bold;
  font-size: 16px;
  text-transform: uppercase;
  letter-spacing: 2px;
}

/* Декоративные золотые тиснения по краям */
.spine::before, .spine::after {
  content: "";
  position: absolute;
  top: 0;
  width: 3px;
  height: 100%;
  background: rgba(212, 175, 55, 0.5); /* Полупрозрачное золото */
}
.spine::before { left: 10px; }
.spine::after { right: 10px; }
